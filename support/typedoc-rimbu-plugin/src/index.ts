import type { Application } from 'typedoc';

export function load(app: Application) {
	// Register router + theme
	// app.renderer.defineRouter('merged-symbols', MergedSymbolsRouter);
	// app.renderer.defineTheme('merged-symbols', MergedSymbolsTheme);
}
