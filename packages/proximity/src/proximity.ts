/**
 * @packageDocumentation
 *
 * The `@rimbu/proximity` package provides the `ProximityMap` implementation: an
 * immutable keyed collection whose `get` and `has` are exact-key lookups, and
 * which additionally supports distance-based nearest-key lookups through
 * `getNearest` and `getNearestMatch`.<br/>
 * <br/>
 * See the [Rimbu docs Map page](https://rimbu.org/docs/collections/map) for more information.
 */

export * from '@rimbu/proximity/map';
